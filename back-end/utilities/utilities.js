module.exports = {
    generateUniqueId: function () {
        //    can be replaced with
        //    read all file titles -> cast to number -> increment -> return
        return Date.now();
    }
};
